
Tag: [[HR параша]] [[BrandNew - Legend]]


### [[РСХБ + Сбер - Resume - Flascards]]


![[RSHB_Sber_resume.pdf]]

июль 2023 — Сентябрь 2025 2 года 2 месяца

**АО «РСХБ»** Россия Финансовый сектор • Банк

**Java-разработчик**

Проект — разработка и развитие цифрового канала дистанционного банковского обслуживания крупного банка: комплекс микросервисов на Java/Spring Boot, развёрнутых в Docker-контейнерах на Kubernetes, с хранением данных в PostgreSQL и асинхронным взаимодействием между сервисами через Apache Kafka. Работал в составе кросс-функциональной продуктовой команды по методологии Scrum.

Разрабатывал и поддерживал REST API сервиса универсальных справочников: 
- реализовывал CRUD-эндпоинты, 
- валидацию входных данных по JSON Schema, 
- версионирование записей без физического удаления для сохранения ссылочной целостности с уже подписанными документами (Java, Spring Boot, Spring Data JPA, Liquibase).

Участвовал в разработке сервиса глобальных настроек платформы: 
- реализовал логику разрешения (resolution) итогового значения параметра с учётом иерархии «глобально - филиал - клиент», 
- добавил REST-эндпоинты для загрузки индивидуальных настроек клиентов пакетно, 
- покрыл бизнес-логику unit- и интеграционными тестами (Spring Boot, JUnit 5, Testcontainers).

Дорабатывал сервис счетов на оплату: 
- реализовал эндпоинты создания и отслеживания статусов счёта, 
- интегрировал сервис с Kafka для публикации событий об изменении статуса оплаты, 
- настроил дедупликацию входящих событий через Redis.

Поддерживал и дорабатывал сервис заявок РКО:
- доработка форм заявок по требованиям бизнес-аналитиков, 
- оптимизация SQL-запросов (JOIN, индексы) для ускорения поиска заявок по клиенту.

Настраивал CI/CD пайплайны для своих сервисов в GitLab CI (сборка Maven, прогон тестов, деплой в Kubernetes через Helm-чарты), участвовал в код-ревью внутри команды.


Достижения: Сократил время отклика API справочников на 35% благодаря добавлению индексов и кэширования часто запрашиваемых значений (Redis). Снизил количество ручных обращений в поддержку по вопросам индивидуальных настроек клиентов за счёт удобного API пакетной загрузки настроек. Повысил стабильность сервиса счетов на оплату — устранил дублирование уведомлений о статусе оплаты благодаря дедупликации событий через Redis. Довёл покрытие unit- и интеграционными тестами ключевой бизнес-логики двух сервисов до 80%+.

Инструменты/Технологии: Java 17, Spring Boot 3, Spring Framework (Core, Data JPA, Web MVC, Security), PostgreSQL 15, Liquibase, Apache Kafka 3, Redis, Docker, Kubernetes, Helm, GitLab CI/CD, Maven, Git, REST API, OpenAPI/Swagger, JUnit 5, Mockito, Testcontainers, SQL (JOIN, индексы).


 октябрь 2025 — июль 2026 10 месяцев

**«Сбер»** Россия Финансовый сектор • Банк

**Java-разработчик**

СберКредит:

Система аналитики для СберКредит: сбор данных по сервисам через Kafka. предоставление на кастомных дашбордах.



Потоковая обработка аудио через FFmpeg - за это никто не шарит


---

**Самая интересная задача:** реализация механизма разрешения индивидуальных настроек клиента с приоритетом над глобальными значениями и автоматической инвалидацией кэша через Kafka-события — задача потребовала разобраться в бизнес-логике сразу нескольких доменов (РКО, ВЭД, продукты), чтобы понять, какие параметры вообще можно переопределять на уровне клиента, и спроектировать API так, чтобы другие команды могли им пользоваться без путаницы.

**Самая сложная задача:** миграция справочника банков-корреспондентов на новую схему версионирования без остановки сервиса и без потери ссылочной целостности — часть уже исполненных платежей в других сервисах ссылалась на старые коды по БИК, поэтому нельзя было просто заменить данные; пришлось разработать схему с параллельным существованием старых и новых записей, аккуратно протестировать миграцию на копии продакшн-данных и синхронизировать выкладку с командой сервиса документооборота, чтобы не сломать уже сформированные исторические выписки.


---
## про дедупликацию через Redis vs стандартный inbox

Это на самом деле важный архитектурный вопрос, и короткий ответ: **Redis-дедупликация — это не замена inbox-паттерна, а более лёгкая и быстрая его версия для определённого класса задач**, с своими компромиссами. Разберу по порядку.

### Как работает классический inbox-паттерн

Идея: при получении события consumer в **той же транзакции**, где обрабатывает бизнес-логику, записывает `message_id` (или его хэш) в таблицу `inbox` с уникальным ограничением (`UNIQUE(message_id)`). Если сообщение придёт повторно (Kafka гарантирует at-least-once, то есть дубликаты — норма при ребалансировке партиций или ретраях), вставка в `inbox` упадёт на уникальном ограничении — значит, событие уже обработано, и обработку можно безопасно пропустить.

**Плюс:** атомарность — либо и бизнес-запись, и отметка о обработке коммитятся вместе, либо не коммитится ничего. Это даёт настоящую консистентность exactly-once на уровне бизнес-эффекта.

**Минус:** каждая проверка — это запись/чтение в реляционной БД внутри транзакции, что добавляет нагрузку на PostgreSQL и немного увеличивает латентность обработки каждого события, особенно при высоком потоке (в резюме упоминается именно высокочастотный поток — статусы платежей, чарджи, рефанды).

### Зачем тогда Redis

Redis используется как **первый, быстрый фильтр** перед тем, как событие вообще дойдёт до бизнес-логики и БД:

```
SET dedup:{message_id} 1 NX EX 300
```

`NX` — записать, только если ключа ещё нет; `EX 300` — TTL 5 минут. Если команда вернула `false` (ключ уже существовал) — событие это дубликат, полученный повторно в короткий промежуток времени (типичная причина — ребалансировка Kafka-consumer group или retry на стороне producer), и его можно отбросить **до** обращения к БД, экономя время и нагрузку на постоянное хранилище.

**Почему это не заменяет inbox полностью:** между `SET NX` в Redis и фактическим коммитом в PostgreSQL нет транзакционной связи. Если процесс упадёт после установки ключа в Redis, но до записи в БД — событие будет потеряно навсегда (при повторной доставке дедупликация его отбросит, решив, что оно уже обработано, хотя фактически бизнес-эффект не наступил). Это классический trade-off "consistency vs latency".

### Правильная комбинация (то, что, скорее всего, реально стоит за формулировкой резюме)

На практике грамотное решение — не либо/либо, а слоёное:

1. **Redis** — быстрый, "оптимистичный" фильтр явных повторов в узком временном окне (TTL 1–5 минут), чтобы не грузить БД одинаковыми событиями при штатных ретраях Kafka.
2. **Идемпотентная запись в БД** как источник истины — не через отдельную inbox-таблицу с message_id, а через **natural key** самой бизнес-сущности (например, `UPSERT` по `(invoice_id, event_type, event_version)` с `ON CONFLICT DO NOTHING` или `DO UPDATE WHEN event_version > current`). Тогда даже если Redis пропустит дубликат по ошибке (TTL истёк, был рестарт Redis без персистентности), повторная обработка не создаст дублирующийся бизнес-эффект — просто ничего не изменится при повторном апдейте той же версии.



- что дальше происходит с сообщениями из dlq. Как смотреть DLQ на проде?
- чекнуть ETL
- Снизил нагрузку на исходные сервисы на ~40% благодаря переходу на event-driven сбор метрик вместо polling REST-эндпоинтов. - в графане смотрели под каждый сервис: CPU, количество потоков, GC

- Сократил время отклика API справочников на 35% благодаря добавлению индексов и кэшированию часто запрашиваемых значений (Redis). -> измеряли через Grafana

- INSERT ON CONFLICT - вместо inbox

- Почему изначально в сервисе справочников не было индексов и кэша? (что за индексы добавил, почему из не было изначально?)

-  Повысил полноту данных до 99,7% за счёт retry-механизма и dead-letter queue для «потерянных» событий -> посчитали долю сообщений, попавших в DLQ, от всех сообщений

- "Ускорил поиск заявок РКО по клиенту: среднее время SQL-запроса снизилось с ~800–900 мс до ~200–250 мс
(pg_stat_statements + логи slow query, Grafana) за счёт оптимизации JOIN и добавления составных индексо" - какие конкретно составные индексы там были?

В DLQ - только сообщения с битой сериализацией.

- **"Какие DLQ-события вызывали потерю данных? Приведите примеры."** → Деталь, которую знает только тот, кто это делал

- **"Как вы справлялись с изменениями схемы событий Kafka во время разработки?"** → Реальная проблема в production

- **"Был ли у вас конфликт между скоростью агрегации в Kafka Streams и размером окна?"** → Классический trade-off

- **Почему вы использовали PostgreSQL 15 вместо Platform V Pangolin?** Была ли это специфика аналитических систем?" → Ответ должен быть технически обоснован


1. Прочекать реальный СберКредит. Подойдет ли описание под него. Описать решение, которое было до нашей реализации. +
2. Просмотреть видос по легенде
3. ответить на частые вопросы
4. Подготовить рассказ по STAR, что внедрили, интересную, сложную задачу


### Базовые вопросы

1. Чем конкретно ты занимался
2. Команда (роли, состав)
3. Производственный процесс (от задачи до прода)
4. Окружения и git-workflow, время релизов
5. Достижения (STAR + ABT рассказы): 5 достижений на каждое место работы, 2 из них с конкретными числовыми метриками (без процентов)
6. Причина увольнения
7. Поведенческие кейсы: Конфликт, Провал, Задача -> Все три истории РАЗНЫЕ: **не дублируют достижения из резюме**

STAR:
S: Situation - что было ДО
T: Task - что нужно сделать
A: Action - что делал + КАК меряли
R: Result - итог + КАК считали

**ABT:**
A: And (И) - как обычно шла работа
B: But (НО) - что изменилось (какая возникла проблема)
T: Therefore (ПОЭТОМУ) - что сделал + к чему привело

Рассказ о РЕШЕНИЯХ, а не о действиях


![[Pasted image 20260731114900.png]]

Конфликт - то, что нельзя решить быстро без привлечения третьей стороны



![[Pasted image 20260731115150.png]]


![[Pasted image 20260731115301.png]]


### Рассказ своей легенды на собесе (техничка):

Кратко (тезисно) пересказываю свое резюме. Где-то чуть больше подробностей добавляю.

В конце "Если у вас есть какие-то вопросы, я готов рассказать подробнее про ту или иную задачу".



"Был сервис системы лояльности, но со временем к нему стали добавляться фичи, новый функционал, он разросся, стал монолитом, стал долго собираться, много команд стало над ним работать и мы начали распиливать его на другие уже микросервисы, чтобы эти фичи выделить"


### Расширенные вопросы по легенде:

1. Какие баги в проде решали?
2. Какой последний баг вы решали?
3. Какой последний баг в проде вы решали?
4. Как анализировали, работали с трассировкой
5. У вас есть эндпоинт и он работает медленно, что будете делать? 
	Ответ подразумевает: метрики буду смотреть (какие?). Как буду делать EXPLAIN, на что ориентироваться? Может лаг в Kafka посмотрю (как?)?
6. Если в K8s пода рестартится, куда будете смотреть?
7. Дежурство
8. Релизный цикл
9. Как задачи оцениваете
10. Как берете задачи из аналитики в разработку
11. Как передаете задачи в тестирование


### Про скатывание с AI помощника

1. Когда рассказываю и читаю с AI помощника инттонация разная? - проверить
2. Контролировать взгляд: не сильно чтобы бегал, но и не в одну точку на экране. Скорее по секунд 5 задерживать - переводить
3. Делать умеренные паузы перед ответом: 2 секунды, чтобы когда чет не знаешь, не выделяться


![[Pasted image 20260731124859.png]]

Для повышения активности можно после отклика на след. день рассылать сообщения "Когда вернетесь с ответом?"